# v0.7.1 Polish Implementation Plan — Final-Review Parked Minors

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Clear the six Minor items the v0.7.0 final whole-branch review parked as deferred (de-duplicated test, hoisted glob constant + stderr fallback in the eslint runner, deduped frontmatter reader in `commands/state.ts`, version string in `bin/loshu-sdlc.ts`, split `captureConsole` into three arrays).

**Architecture:** Each fix is a tightly-scoped edit to one file (occasionally two). All fixes are independent — they touch different files (with two sharing `commands/rules.ts`). The fixes do not introduce new abstractions; they DRY up duplicated literals and replace hardcoded values with the canonical helpers already established in v0.6.4 / v0.7.0. No new tests are needed for the helper-reuse fixes because the existing tests already exercise the underlying helper (`readFrontmatterFile` in Task 3's `frontmatter.test.ts`; `currentCliVersion` in Task 11's `upgrade.test.ts`). The two fixes that change observable behavior (`#3` stderr fallback, `#5` `--version` output) get a small new test.

**Tech Stack:** Node ≥ 20.0.0, TypeScript 5.4 strict, vitest 1.6, fs-extra 11 (sync `readJsonSync`), `yaml` 2.4, pnpm 9 workspaces. No new dependencies.

**Spec:** The plan does not introduce a new spec doc — the six items originate from the v0.7.0 final whole-branch review (per `D:\workspace\3.my\SDLC\.superpowers\sdd\v0.7.0-real-stubs\progress.md` "Final Whole-Branch Review" section and the original Task 5 review). The plan's source of truth is that review record plus the existing source files.

## Global Constraints

- Node ≥ 20.0.0 must work (CI matrix runs Node 20).
- pnpm invoked via `npx pnpm@9.0.0 <cmd>` in sandboxes without global pnpm.
- TypeScript strict: `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess` — optional fields need `| undefined`, regex match groups need `!` or `?? fallback`.
- `packages/cli` is `"type": "module"` — ESM imports only.
- Conventional Commits (`feat:` / `fix:` / `test:` / `docs:` / `chore:` / `refactor:`).
- Test framework: vitest only; test files live at `packages/cli/tests/**`.
- Bash hooks: untouched in this plan.
- All work on `main`; one commit per logical fix (six fix commits + one CHANGELOG commit).
- Never paste tokens/secrets into any file, commit, or report.
- **Base commit for this plan:** `83fa3ad` (the post-final-review-fix HEAD on `main`). The plan's diff is `83fa3ad..HEAD` at completion.
- **vitest pool:** `vitest.config.ts` is `pool: 'forks'` (set by v0.7.0 Task 5). New tests must work under that pool. If a new test needs `process.chdir()`, it is compatible with the forks pool.

---

## File Structure (mapped before tasks)

**Modified files (5):**
- `packages/cli/src/commands/rules.ts` — hoist `ESLINT_GLOBS` module constant; fix `??` → `||` fallback in eslint runner.
- `packages/cli/src/commands/state.ts` — replace local `readFileSync + parseYaml` with `readFrontmatterFile` import from `lib/accept/frontmatter.js`.
- `packages/cli/src/bin/loshu-sdlc.ts` — extract `currentCliVersion()` helper (same pattern as v0.7.0 Task 11 upgrade default); use it for `--version`.
- `packages/cli/tests/commands/rules.test.ts` — delete the duplicate `check returns 2 for an unknown rule` test at lines 57-68 (kept inside the borrowed/unknown describe at line 328-337).
- `packages/cli/tests/commands/git.test.ts` — split `captureConsole` into `logs: string[]`, `errors: string[]`, `warns: string[]`; production `console.warn` should land in `warns` not `errors`.

**New tests (2, both additions):**
- A small test asserting `loshu-sdlc --version` outputs the CLI's current package.json version (covers fix #5's behavior change).
- A small test that the eslint runner's catch path falls through to stderr when stdout is empty (covers fix #3's behavior change). The existing `tests/commands/rules.test.ts` eslint-fail test already inspects captured output; adding one more case for the empty-stdout scenario is enough.

**Docs (1 modified):**
- `CHANGELOG.md` — v0.7.1 entry above v0.7.0.

**Release (1 commit via `scripts/release.mjs`):**
- v0.7.1 → bump versions, run gauntlet, lockfile, `chore: release v0.7.1` commit; controller manually creates local `v0.7.1` tag.

---

## Task Index

| # | Task | Files | Status |
|---|------|-------|--------|
| 1 | [Remove duplicate unknown-rule test](task-01-remove-dup-unknown-test.md) | `tests/commands/rules.test.ts` | ☐ |
| 2 | [Hoist ESLINT_GLOBS + fix stderr fallback in eslint runner](task-02-eslint-runner-polish.md) | `commands/rules.ts`, `tests/commands/rules.test.ts` | ☐ |
| 3 | [Dedupe frontmatter reader in commands/state.ts](task-03-state-frontmatter-dedupe.md) | `commands/state.ts` | ☐ |
| 4 | [Read CLI version in bin/loshu-sdlc.ts --version](task-04-bin-version-dynamic.md) | `bin/loshu-sdlc.ts`, new test | ☐ |
| 5 | [Split captureConsole into logs/errors/warns arrays](task-05-split-capture-console.md) | `tests/commands/git.test.ts` | ☐ |
| 6 | [CHANGELOG v0.7.1 entry](task-06-changelog.md) | `CHANGELOG.md` | ☐ |
| 7 | [Release v0.7.1](task-07-release.md) | (release.mjs) | ☐ |

**Parallelism:**
- Tasks 1, 3, 4, 5 are fully independent (different files).
- Task 2 is sequential after Task 1 (both touch `tests/commands/rules.test.ts`); no dependency on Tasks 3-5.
- Task 6 is sequential after all five fix commits (5 → 6).
- Task 7 is sequential after Task 6 (6 → 7).

Recommended order: 1 → 2 (sequential on `rules.test.ts`) ∥ 3 ∥ 4 ∥ 5 → 6 → 7.

## Verification (final gate — Task 7 runs this)

```bash
npx pnpm@9.0.0 typecheck
npx pnpm@9.0.0 test            # 228 → ≥230 (or whatever the new total is); 0 skips; 0 regressions
npx pnpm@9.0.0 build
npx pnpm@9.0.0 test:eval:strict # 30/30 still passing
npx pnpm@9.0.0 lint            # 0 errors
```

Plus manual smoke: `node packages/cli/dist/bin/loshu-sdlc.js --version` should print the CLI's current package.json version (verifies Task 4 fix).

## Self-Review Checklist

- [x] Every parked Minor from v0.7.0 final review mapped to a task (#1 → T1, #2 → T2, #3 → T2, #4 → T3, #5 → T4, #6 → T5).
- [x] Every task names exact file paths.
- [x] Tasks ordered by dependency DAG; no task depends on a later task.
- [x] Each task's "Files" block names exact paths.
- [x] Each task's "Interfaces" block names exact signatures shared with neighboring tasks.
- [x] Steps are 2-5 minutes each; one action per step.
- [x] Conventional Commits throughout.
- [x] No placeholder language (TBD, TODO, "add appropriate error handling", "similar to Task N").
- [x] Verification gauntlet defined.
- [x] Manual smoke test added for the one observable-behavior change (#5 → `--version`).

## Cross-References

- Source of truth: v0.7.0 final whole-branch review (per `.superpowers/sdd/v0.7.0-real-stubs/progress.md`)
- Existing `readFrontmatterFile` helper: `packages/cli/src/lib/accept/frontmatter.ts` (v0.7.0 Task 3)
- Existing `currentCliVersion` pattern: `packages/cli/src/commands/upgrade.ts:36-52` (v0.7.0 Task 11) — replicate this exact pattern in `bin/loshu-sdlc.ts`
- Existing vitest config (`pool: 'forks'`): `vitest.config.ts:9` (v0.7.0 Task 5)
- Existing `RULES` array (where eslint rule's `appliesTo` is declared): `packages/cli/src/commands/rules.ts:74-107`
- Existing `captureConsole` (where `console.warn` is incorrectly captured into `errors`): `packages/cli/tests/commands/git.test.ts:36-54`